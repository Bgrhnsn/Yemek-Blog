<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <title>Yemek Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container py-4">
    <h1 class="text-center text-primary mb-4">🍲 Yemek Tarifi Blog</h1>

    <p class="text-end text-muted">
      Toplam ziyaret: <strong><%= visitCount %></strong> <br>
      Şu anda <strong id="onlineCount">0</strong> kişi çevrimiçi
    </p>

    <h2 class="mt-5">📢 Duyurular</h2>
    <% if (announcements.length > 0) { %>
      <% announcements.forEach(a => { %>
        <div class="card my-3">
          <div class="card-body">
            <h5 class="card-title"><%= a.title %></h5>
            <p class="card-text"><%= a.content %></p>
            <p class="card-text"><small class="text-muted"><%= a.createdAt.toLocaleString() %></small></p>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <div class="alert alert-info">Henüz duyuru yok.</div>
    <% } %>

    <a href="/recipes" class="btn btn-primary mt-4">Tarifleri Gör</a>
  </div>

  <script src="/socket.io/socket.io.js"></script>
  <script src="/js/socket.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
